export const ANIMATION_DURATION = {
  fast: 200,
  normal: 300,
  slow: 500,
} as const;

export const CAROUSEL_INTERVAL = 5000;

export const SCROLL_PICKER = {
  itemHeight: 56,
  visibleItems: 5,
  paddingItems: 2,
} as const;

export const DEBOUNCE_DELAY = {
  search: 300,
  scroll: 100,
  resize: 150,
} as const;
